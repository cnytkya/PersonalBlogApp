/**
 * API'den gelen veya API'ye gönderilen (Güncelleme)
 * tam Category nesnesi.
 */
export interface Category {
  id: number;
  name: string;
  // 'blogs' listesini admin panelinde (şimdilik)
  // yönetmeyeceğimiz için buraya eklemiyoruz.
}